<mirror>
	<id>maven2</id>
	<mirrorOf>*</mirrorOf>
	<name>maven</name>
	<url>http://repo1.maven.org/maven2 </url>
</mirror>

在settting.xml 吧除了上面的镜像地址都删除了安装完了在添加回来

如果说mvn 不是一个自定义命令啥的，就把maven的 bin目录配置到path中
