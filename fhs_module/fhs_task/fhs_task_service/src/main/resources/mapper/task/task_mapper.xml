<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.fhs.task.mapper.TaskMapper">


    <select id="getTaskVO" resultType="com.fhs.task.vo.TaskVO">
       SELECT
			job.JOB_NAME AS jobName,
			job.JOB_GROUP AS jobGroup,
			job.DESCRIPTION AS description,
			job.JOB_CLASS_NAME AS jobClassName,
			cron.CRON_EXPRESSION AS cronExpression,
			tri.TRIGGER_NAME AS triggerName,
			tri.TRIGGER_STATE AS triggerState,
			job.JOB_NAME AS oldJobName,
			job.JOB_GROUP AS oldJobGroup
		FROM
			qrtz_job_details AS job
			LEFT JOIN qrtz_triggers AS tri ON job.JOB_NAME = tri.JOB_NAME
			LEFT JOIN qrtz_cron_triggers AS cron ON cron.TRIGGER_NAME = tri.TRIGGER_NAME
		WHERE
			tri.TRIGGER_TYPE = 'CRON'
			<include refid="where"/>
            LIMIT ${start},${pageSize}
    </select>
    <select id="getTaskCount" resultType="java.lang.Integer">
	SELECT
		count(1)
	FROM
		qrtz_job_details AS job
		LEFT JOIN qrtz_triggers AS tri ON job.JOB_NAME = tri.JOB_NAME
		LEFT JOIN qrtz_cron_triggers AS cron ON cron.TRIGGER_NAME = tri.TRIGGER_NAME
	WHERE
		tri.TRIGGER_TYPE = 'CRON'
		<include refid="where"/>
    </select>

	<sql id="where">
		<if test="task.jobName != null and task.jobName != ''">
			AND job.JOB_NAME LIKE CONCAT(#{task.jobName}, '%')
		</if>
		<if test="task.jobGroup != null and task.jobGroup != ''">
			AND job.JOB_GROUP LIKE CONCAT(#{task.jobGroup}, '%')
		</if>
		<if test="task.jobClassName != null and task.jobClassName != ''">
			AND job.JOB_CLASS_NAME LIKE CONCAT(#{task.jobClassName}, '%')
		</if>
		<if test="task.description != null and task.description != ''">
			AND job.DESCRIPTION LIKE CONCAT(#{task.description}, '%')
		</if>
	</sql>
</mapper>